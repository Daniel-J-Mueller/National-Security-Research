# Private Electrical Data

This category stores exact-coordinate electrical outputs and private working files.

Current subfolders:

- `eia860_2024/`
- `visualizer/`

The local visualizer build still writes `visualizer/plants_reference.json`, and
the shared multi-category visualizer reads from `data/private/visualizer/`.
